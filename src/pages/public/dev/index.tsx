import { Ripple } from "@/components/ui/shadcn-io/ripple";

export default function App() {
  return (
    <main className="container space-y-4 p-[clamp(1rem,4vw,3rem)]">
      <Ripple />
    </main>
  );
}
